#!/usr/bin/env ruby

require 'sticky_fingers'
require 'slop'

opts = Slop.parse(help: true, ignore_case: true, optional_arguments: true) do
  on :d=, 'exdir'
  on :dry_run, 'dry run'
  on :q, 'quiet'
end

filename = ARGV.shift
exdir = exdir || '.'

StickyFingers.unzip(filename, exdir)
